# Harris Agent 任务单：压模 S1-S7 增量增强改造

- task_id: harness-enhance-v1
- version: 1.0
- date: 2026-09-09
- 关联文档：docs/harness/00_基线审计报告（只读）+ docs/harness/01_增量改造完整方案 V1.0（唯一设计依据）
- 文件名规范：全短横线，禁中文空格；原名归档存 docs/harness/_backup/

## 输入材料
- docs/harness/00_压模程序-S1-S7现状审计报告-2026-09-09.md（read_only 基线）
- docs/harness/01_压模程序-S1-S7-增量改造完整方案-V1.0.md（primary_design）

## 核心总约束（最高优先级）
1. 不推翻现有 S1-S7 DFA 状态机，不打乱已跑通主流程；全部增量补强。
2. 严格区分优先级：🔴紧急 + 🟠高 = 第一批次完整实现；🟡中 = 仅 schema/yaml 骨架/接口/注释文档，**严禁接入正式流水线**；🟢远期 = 仅设计备注不编码。
3. 所有变更附带验收标准；每个新守卫/schema/gate 有可验证测试用例。
4. 新增数据库/台账（tech_debt_ledger、S7-B、overlay）全部向后兼容，历史 run 卷宗可读。
5. 严禁"机器硬闸门"退化为 prompt 软约束——硬闸必须实现在引擎/Delegate 层。
6. 所有变更同步输出变更清单：改动文件/新增文件/删除文件/风险点/回归测试项。

## 交付产物
1. 全部源码变更（引擎/FlowEngine/DelegateReviewer/YAML/schema/overlay 骨架/DB 建表脚本）
2. 配套单测 + 端到端回归用例（覆盖全部🔴+🟠）
3. docs/harness/改造差异报告.md（逐条对照 00 基线说明缺口如何补齐；中/远期项骨架预留说明）
4. 运行验证说明文档（如何跑端到端，验证补丁循环拦截/S7归档校验/证据完整注入/S3大文件覆写拦截/债务台账强制登记）
5. git 变更清单（改动/新增/删除/风险点/回归项）

## 禁止行为
- ❌ 自行调整 S1-S7 阶段顺序
- ❌ 重构/删除已验证可用的原有守卫、DFA 流转
- ❌ 把🟡中、🟢远期需求混入本次上线实现
- ❌ 忽略向后兼容，破坏历史审计卷宗解析
- ❌ 机器硬闸退化为 work_manual 文字提示

## 推荐执行顺序
1. 通读两份输入 → 产出初步文件变更清单 → **先向人确认，再动手编码**
2. 完成骨架、DB schema、overlay 契约定义后，再次同步确认
3. 再实现🔴紧急业务逻辑
4. 再实现🟠高优先级业务逻辑
5. 编写全部单元测试、端到端回归用例
6. 运行端到端验证
7. 输出全部交付产物

## 完成定义
- 全部🔴+🟠验收标准通过
- 🟡仅骨架与文档不接入流水线
- 🟢仅注释
- 回归用例全部通过
- 交付产物 5 项全部产出
- git 变更清单清晰可审
