# S6/S7 拆分实施设计（H-02/H-01 收口）— ✅ 已执行完毕（2026-09-10）

> **状态：已全部实施并验证通过。** `tsc --noEmit` 0 错误；`vitest run` 597/597 通过；mcp 已重启加载。
> 实施结果与偏差记录见 `04_改造差异报告-enhance-v1.md`（四）、运行验证见 `05_运行验证说明-enhance-v1.md`。
> 🔴 两点与本文档原设计的偏差（已按架构实际落地）：
> 1. **delegate 注册位置**：实际在 `mcp/server.ts` 的 `delegateFnMap`（run handler 内构建），
>    非 StageRunner/DelegateReviewer 集中注册处。
> 2. **「真阻塞」不可实现**：FlowEngine 是单程 `start()` 递归 DFA，**无运行中暂停/恢复原语**，
>    且 run_id 每次调用新生成 → 改为「**终局悬挂 + 确认后重跑**」，任务单按
>    **change_key（变更指纹）** 而非 run_id 寻址。详见 `04_`（四）H-02 行。
>
> 以下为原始设计（保留溯源）。
>
> 前置类型/模块已就绪：run_status(FlowResult 扩展)、S7ArchiveValidator(+6测试)、manual_verification_ticket schema。
> 风险提示：S7/S6 是 agents 主路径；任何一半落地都会破坏流程 → 一次专注窗口做完 + 干净回归后再 mcp 重启。

## 1. YAML 变更（wenstaros_core_repair_flow.yaml）
- **S7 拆分**：现 `S7_Change_Archive`（gate auto）→
  - `S7-A_Change_Archive`：原 S7 逻辑；work_manual 增"S7-A 必须产出结构化 s7_archive_payload（字段见 01 方案 §4.2）写入 data/archives/<run_id>.json"；gate auto → next `S7-B_Archive_Validate`
  - `S7-B_Archive_Validate`【新增】runner_mode **delegate**（进程内 S7ArchiveValidator），gate condition；pass → next END / fail → next S7-A_Change_Archive；旧 S7 逻辑用 `# DEPRECATED-NOTE: 原 S7 逻辑迁移至 S7-A` 保留溯源
- **S6 拆分**：现 `S6_Function_Verify`（本地脚本 gate condition）→
  - `S6-A_Function_Verify_Machine`：现 S6 全部脚本硬校验（tsc/vitest/CK-09/CK-10/save grep）保持 condition（失败→S3）；pass → next `S6-B_Manual_Verify`
  - `S6-B_Manual_Verify`【新增】gate condition；若 s2_evidence.skip_manual_verification=true → 自动 pass；否则生成 manual_verification_ticket 存入卷宗，run_status=await_manual_verification 阻塞（人工确认签名后 condition pass → S7）；旧 S6 原注释改 `# DEPRECATED-NOTE`
- 顶层注释补两配置职责（E-01 已做）

## 2. 代码接线（与 YAML 同批）
- `src/StageRunner.ts`：delegateReviewFnMap 注册
  - `S7_B_Archive_Validate` → (stage,state)=>校验器：读 data/archives/<run_id>.json → validateArchive(payload,{diffFiles:state.modified_files, s45score:末轮compliance, isLargeRefactor, ledgerGetDebt, exemptionExists}) → pass/reject 双通道
  - `S6_B_Manual_Verify` → 读卷宗 ticket 全 confirmed → pass；否则 run_status=await_manual_verification + 人工确认检查
- `src/FlowEngine.ts`：
  - deriveFinalResult/buildResult：run_status 已支持 completed/aborted；S6-B/S7-B 置 await_manual_verification/archive_invalid（通过 stage 完成后写 state 标记再派生）
  - 旧卷宗兼容：老 run 无 S6-B/S7-B → run_status 默认 completed/aborted（已实现）
- `src/DelegateReviewer.ts`：S7-B 校验 delegate 集中注册处（复用 S7ArchiveValidator）
- `mcp/server.ts`：FlowResult.run_status 透出到 MCP 返回；await_manual_verification 在状态面板区分（非 error）

## 3. run_status 语义（H-02/H-01 已预留）
completed / aborted / await_manual_verification（S6-B 未确认）/ archive_invalid（S7-B 校验失败返 S7-A）——扩展枚举，不改旧解析（未知状态降级读卷宗）。

## 4. 回归（专注窗口执行）
- E02/H01/H02 用例（01 方案 §5）：S7-B 拦截缺债务/缺豁免/回滚漏记；S6-A 失败回流；S6-A 过+S6-B 未确认→await_manual；skip_manual_verification=true→全自动 completed
- 旧卷宗解析兼容测试（跑 2 份历史 run）
- 正向一次通过回归
- 全量 vitest + mcp 重启激活

## 5. 前置已完成（本会话）
run_status 字段(FlowEngine FlowResult)｜S7ArchiveValidator.ts(R1-R5,6测试)｜manual_verification_ticket schema+json｜s2-evidence-v2 schema（skip_manual_verification 字段）
